---
description: toolkit 架构约定 — Hono 单端口、prompt 目录、feature 分层、代码干净
alwaysApply: true
---

# toolkit 结构

## CLI

- 前缀 `tkt`；一能力一目录：`src/features/<name>/`
- Agent 只调 CLI；不要再包一层 Skill 壳去转发同一套命令
- 用户数据：`~/.config/tkt/<命令名>/`，分文件存，不写进仓库

## Feature 分层（勿平铺散乱）

复杂 feature **按阶段分子目录**，根目录只留入口与共享契约：

```
features/<name>/
  index.ts          # CLI 注册
  run.ts            # 编排流水线
  types.ts          # 共享类型 / schema
  errors.ts         # （可选）领域错误
  <stage>/          # 按职责：config|prefs|gather|collect|ai|git|deliver…
```

参考：

- `report/`：`config/` → `gather/` → `ai/` → `deliver/`
- `git-submit/`：`prefs/` → `collect/` → `ai/` → `git/`

约定：

- **禁止**在 feature 根堆一堆平级 `.ts`（难维护）
- 新能力照现有 feature 拆阶段；简单工具（一两文件）可不拆
- 阶段目录用 `index.ts` 对外导出；编排只从阶段目录 import
- 共享路径用 `src/core/paths.ts`，不要各写一套找 package root

## Web（Hono）

- **只开一个端口**（默认 `127.0.0.1:8787`，`tkt ui`）
- 各工具用路由区分，不要另起进程/端口：
  - 页面：`/<name>`（如 `/bench`）
  - API：`/api/<name>/*`
- Hono 做本机网关：**反向代理 / 挂载**各 feature 的页面与 API；业务逻辑留在 `features/`，`src/server/` 只负责组 app、listen
- 新工具：在 feature 里 `mount(app)`，登记到 `src/server/index.ts` 的 mounts 列表
- 不引入 Express / Vite 当本地工具 UI 壳

## Prompt

- **所有提示词**放 `prompts/`（按能力分子目录），经 `PROMPT_CATALOG` 注册
- feature / 本地 AI 用 `loadPrompt(id)`；改文案只动 `prompts/`
- 需要模型推理的能力（`tkt gc` / `tkt report`）走 **本地 AI SDK 编排**；纯 UI 工具（bench）页面自配，不配 Agent prompt
- DeepSeek 等网关拒 `json_schema`：`supportsStructuredOutputs` 按模型自动关（回退 `json_object`）；可用 `AI_STRUCTURED_OUTPUTS` 强制
- `AI_BASE_URL` 若只写 host，客户端会补 `/v1`
- `tkt report` 启动交互名单：`alias | display_name | enabled`；空格取消不采集；附带信息进 Append；`display_name` 按 remote 猜一次后记住
- report AI tool `add_repo`：先校验本地路径是 git，否则直接失败不写库；是则入库并采 commit，合并进当日 gather 再出日报

## 代码干净

- 改动只碰相关文件；不顺手大重构、不堆无关注释/文档
- feature 内：CLI / 编排 / 阶段逻辑分文件；共享路径用 `src/core/paths.ts`
- 依赖尽量薄；本地 UI 服务坚持 Hono + 随包 `assets/`
